using GameBuilder.Library;
using GameBuilder.Library.Graphics;

namespace TestGame1.States
{
    public class GameState : State
    {
        public GameState(GraphicsManager.RenderMode renderMode, bool overlay) : base(renderMode, overlay)
        {
        }
    }
}
